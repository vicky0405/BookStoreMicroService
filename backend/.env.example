# Environment
NODE_ENV=development
PORT=5000

# Database (MSSQL)
# If using a NAMED INSTANCE (e.g. DESKTOP-ABC\MSSQLSERVER01) set DB_HOST to the machine name
# and DB_INSTANCE to the instance part (MSSQLSERVER01). Leave DB_PORT commented unless you
# configured a STATIC TCP port. If you do not set DB_PORT, ensure SQL Browser service is running.
DB_HOST=DESKTOP-MOFH91GMSSQLSERVER01
# Default instance in your case; leave DB_INSTANCE blank
#DB_INSTANCE=
DB_PORT=1433
DB_NAME=mainservice
DB_USER=root
DB_PASSWORD=123456
DB_ENCRYPT=false

# Redis (optional)
REDIS_HOST=your-redis-host
REDIS_PORT=6379

# Services URLs
# Point to other microservices (adjust ports if changed)
USER_SERVICE_URL= http://localhost:5001
ORDER_SERVICE_URL= http://localhost:5002